apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: prometheus
  namespace: monitoring
spec:
  replicas: 1
  serviceMonitorSelector: 
    metrics: prometheus
  ruleSelector: 
    metrics: prometheus
  podMonitorSelector: 
    metrics: prometheus
  probeSelector: 
    metrics: prometheus
  # alerting:
  #   alertmanagers:
  #     - namespace: monitoring
  #       name: alertmanager-main
  #       port: web
